// Export types
export type {
  Product,
  ProductInput,
  InventoryState,
  InventoryFilters,
  InventoryPagination,
  InventoryStats,
} from './types';

// Export components
export { AddProductForm, UpdateProductForm } from './components';

// Export hooks (to be created)
// export { useInventory } from './hooks/useInventory';

// Export store (to be created)
// export { fetchProducts, createProduct, updateProduct, deleteProduct } from './store/inventorySlice';
